# Schemas

Versioned JSON schemas for the analyzer's canonical machine-readable
output. `analysis-result.schema.json` (schema version 1) is added with
the canonical JSON report implementation. Schema changes require
deliberate versioning; consumers must not be broken silently.
