# Examples

Real, runnable examples of the `analyze` CLI command.

- [`upgrade-review/`](upgrade-review/) -- a full walkthrough: two real
  fixture executables, the exact command that was run against them, the
  actual captured terminal and JSON output, and an explanation of what
  each finding means.

More examples (a manifest-driven migration-required case, a rehearsal
run) may be added the same way: real fixtures, a real command, real
captured output.
