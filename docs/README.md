# Documentation

This directory holds design and reference documentation for the
SorobanLabs Analyzer that does not belong in the top-level README:
the confidence and finding model, rule references, the report schema,
and fixture structure.

Documentation is added alongside the implementation step that it
describes; see the top-level README for what is implemented today.
