# Workspace integration tests

Cross-crate integration tests, including fixture-driven analysis tests,
JSON schema validation tests, and determinism/reproducibility tests.
Populated alongside the corresponding implementation steps.
